# Данные пользователей: прогресс сборки — не коммитить
data/

# Секреты для docker-compose.prod.yml (ADMIN_INTERNAL_KEY) — подставляются
# через ${VAR:-}, живут только на сервере, права 600
.env

# Локальный запуск: свои базы auth и сервиса (node dev.mjs)
.dev/

# Рабочие базы e2e-теста (test/e2e-rooms.mjs) — создаются и удаляются
# при каждом запуске, коммитить нечего (случайно попадали в git — исправлено)
test/.work/

# Артефакты ОС/редакторов
*.log
.DS_Store
Thumbs.db
